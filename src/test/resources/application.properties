# Test-only overrides. The PostgreSQL container is wired via @ServiceConnection.
# A fake JWKS URI is enough because NimbusJwtDecoder fetches keys lazily and
# all controller-slice tests bypass JWT decoding via SecurityMockMvcRequestPostProcessors.jwt().
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost/jwks
onlyclimb.clerk.webhook-signing-secret=whsec_dGVzdHNlY3JldA==
